<Page
    x:Class="AudioCumulus.MusicPlayer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AudioCumulus"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Background="#111321">
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="90"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Background="{StaticResource SystemAccentColorDark2}" Grid.ColumnSpan="2"/>
        <TextBlock Style="{StaticResource HeaderTextBlockStyle}" Text="List of Music" Grid.Column="1"/>

        <Grid Grid.Row="1" Grid.Column="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="0.5*"/>
            </Grid.ColumnDefinitions>
            
            <VariableSizedWrapGrid Grid.Column="0">
                <ListView x:Name="myList" Height="Auto" Width="Auto" ItemsSource="{x:Bind MusicList}" SelectionChanged="ListView_SelectionChanged">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding fileName}"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Artist}"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Album}"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Duration}"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding AlbumCover}"/>
                                </StackPanel>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </VariableSizedWrapGrid>

            <StackPanel Background="{StaticResource SystemAccentColorDark2}" Grid.Column="1"/>
            <VariableSizedWrapGrid Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Button Height="50" Width="150" Click="musicPlayer_Click" Content="Open File"/>
            </VariableSizedWrapGrid>
        
        </Grid>
        
        <MediaPlayerElement x:Name="musicPlayer"
                      Grid.Row="3"
                      Grid.ColumnSpan="3"
                      AreTransportControlsEnabled="True"
                      Source="{Binding MusicPath}">
            <MediaPlayerElement.TransportControls>
                <MediaTransportControls VerticalAlignment="Bottom"
                                        Height="90"
                                        IsPlaybackRateButtonVisible="True"
                                        IsPlaybackRateEnabled="True"
                                        IsFastForwardButtonVisible="True"
                                        IsFastForwardEnabled="True"
                                        IsFastRewindButtonVisible="True"
                                        IsFastRewindEnabled="True"
                                        IsStopButtonVisible="True"
                                        IsStopEnabled="True"/>
            </MediaPlayerElement.TransportControls>
        </MediaPlayerElement>
        
    </Grid>
</Page>
