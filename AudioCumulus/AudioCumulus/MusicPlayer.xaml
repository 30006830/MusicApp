<Page
    x:Class="AudioCumulus.MusicPlayer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AudioCumulus"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    RequestedTheme="Dark">

    <Grid Background="#111321">
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="90"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <TextBlock Style="{StaticResource HeaderTextBlockStyle}" Text="List of Music" Grid.Column="1"/>

        <VariableSizedWrapGrid Grid.Row="1" Grid.Column="1">
            <Button Height="50" Width="150" Click="musicPlayer_Click"/>
            <ListView ItemsSource="{x:Bind MusicList}" Grid.ColumnSpan="2" Grid.Row="1">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="File Name: "/>
                            <TextBlock Text="{Binding fileName}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Artist: "/>
                            <TextBlock Text="{Binding Artist}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Album: "/>
                            <TextBlock Text="{Binding Album}"/>
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        </VariableSizedWrapGrid>
        <MediaElement x:Name="musicPlayer"
                      Grid.RowSpan="3"
                      Grid.ColumnSpan="3"
                      AreTransportControlsEnabled="True">
            <MediaElement.TransportControls>
                <MediaTransportControls VerticalAlignment="Bottom"
                                        Height="90"
                                        IsPlaybackRateButtonVisible="True"
                                        IsPlaybackRateEnabled="True"
                                        IsFastForwardButtonVisible="True"
                                        IsFastForwardEnabled="True"
                                        IsFastRewindButtonVisible="True"
                                        IsFastRewindEnabled="True"
                                        IsStopButtonVisible="True"
                                        IsStopEnabled="True"/>
            </MediaElement.TransportControls>
        </MediaElement>
    </Grid>
</Page>
